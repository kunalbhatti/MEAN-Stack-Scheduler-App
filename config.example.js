module.exports = {
    'secret': 'secret-key', // for JWT
    'api_key': 'api-key', // we get the api key from sendgrid
    'from': 'xyz@abc.com', // from email will be sent from this mail id. This id will have to be registered in sendgrid or some other service like it
    'db_link': 'db_link' // mongo db link
}